<!DOCTYPE html>
<html lang="en">
{% include header.ext %}
<!-- NAVBAR
================================================== -->
  <body>

    {% include navbar.ext %}

    <div class="container">
        <div class="well well-sm">
            <h1>{{ page.title }}</h1>
            {{ content }}
            <span class="pull-right">Veröffentlicht am {{ page.date  | date: "%d.%m.%Y" }} von {{ page.author }}</span>
            <br>
        </div>
    </div>



    {% include footer.ext %}

    {% include bsincludes.ext %}
    {% include google-analytics.ext %}
  </body>
</html>

